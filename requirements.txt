mlx>=0.16.0
numpy>=1.24
# OpenEXR I/O (recommended for VFX pipelines)
OpenEXR>=3.2.0
# Fallback I/O if OpenEXR unavailable
# opencv-python>=4.8
# PyTorch only needed for first-run .pth conversion (not inference)
# torch>=2.1
