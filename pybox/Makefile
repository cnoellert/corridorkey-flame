INSTALL_DIR = /opt/corridorkey/pybox
FILES       = corridorkey_pybox.py corridorkey_daemon.py

.PHONY: install
install:
	mkdir -p $(INSTALL_DIR)
	cp $(FILES) $(INSTALL_DIR)/
	@echo "Installed to $(INSTALL_DIR)"
